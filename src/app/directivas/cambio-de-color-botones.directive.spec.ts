import { CambioDeColorBotonesDirective } from './cambio-de-color-botones.directive';

describe('CambioDeColorBotonesDirective', () => {
  it('should create an instance', () => {
    const directive = new CambioDeColorBotonesDirective();
    expect(directive).toBeTruthy();
  });
});
