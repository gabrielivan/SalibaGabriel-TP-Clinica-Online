import { FiltrarHorariosPipe } from './filtrar-horarios.pipe';

describe('FiltrarHorariosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarHorariosPipe();
    expect(pipe).toBeTruthy();
  });
});
